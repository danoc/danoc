{
  "private": true,
  "scripts": {
    "start": "gatsby build && gatsby develop",
    "precommit": "lint-staged",
    "test": "eslint .",
    "deploy": "rm -rf public/ && gatsby build && netlify deploy"
  },
  "dependencies": {
    "gatsby": "^1.9.14",
    "gatsby-link": "^1.6.14",
    "gatsby-plugin-react-helmet": "^1.0.4",
    "gatsby-plugin-styled-components": "^1.0.4",
    "gatsby-source-filesystem": "^1.4.7",
    "gatsby-transformer-remark": "^1.7.4"
  },
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-prettier": "^2.3.0",
    "husky": "^0.14.3",
    "lint-staged": "^4.0.4",
    "netlify-cli": "^1.2.2",
    "prettier": "^1.6.1"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "git add"
    ]
  }
}
