{
  "private": true,
  "scripts": {
    "start": "gatsby build && gatsby develop",
    "precommit": "lint-staged",
    "test": "eslint .",
    "deploy": "npm install && gulp && cp .htaccess dist/.htaccess && rsync -r --progress --delete dist/ daniel@danoc.me:/var/www/danoc.me/public"
  },
  "dependencies": {
    "gatsby": "^1.6.6",
    "gatsby-link": "^1.6.5",
    "gatsby-plugin-react-helmet": "^1.0.1",
    "gatsby-plugin-styled-components": "^1.0.1",
    "gatsby-source-filesystem": "^1.4.1",
    "gatsby-transformer-remark": "^1.6.3"
  },
  "devDependencies": {
    "eslint": "^4.4.0",
    "eslint-config-prettier": "^2.3.0",
    "eslint-config-standard": "^10.2.1",
    "husky": "^0.14.3",
    "lint-staged": "^4.0.2",
    "prettier": "^1.5.3"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "git add"
    ]
  }
}
